name: Ping Service

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Website
        run: curl -s -o /dev/null -w "%{http_code}" https://${{secrets.BASE_URL}}/health